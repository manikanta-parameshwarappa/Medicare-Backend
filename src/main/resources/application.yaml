spring:
  application:
    name: Medicare
  datasource:
    url: jdbc:postgresql://localhost:5432/medicare?useSSL=false
    username:
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
      show-sql: true
    properties:
      hibernate:
        format_sql: true
  security:
    user:
      name: admin
      password: admin123
jwt:
  secret: my-secret-key-of-at-least-32-characters